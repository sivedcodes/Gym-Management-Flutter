# Task log

- [x] **F16 — UI polish + web branding**: feet/inches profile setup, smaller centralized typography, Total Fit Gym favicon/PWA icons, and `sived.codes` developer credit.
- [x] **F17 — Render build support**: pinned Flutter SDK bootstrap script for Render's native build environment; Render builds publish `build/web`.
- [x] **F18 — History separate screen & dark mode input typography**: History option placed under 'My gym timing' in Profile screen opening dedicated HistoryScreen with Logout placed directly underneath; dark mode white text styles enforced across all input fields, dialogs, and dropdowns.
